<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Page</title>
    <script>
        const ROOM_ID = "<%= roomId %>";
    </script>
    <script defer src="https://unpkg.com/peerjs@1.4.7/dist/peerjs.min.js"></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="script.js" defer></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        /* Add your custom styles here */
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            background-image: url("https://wallpapercave.com/dwp2x/wp2661915.jpg");
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }

        #video_grid {
            width:100%;
            margin: auto;
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            grid-template-rows: auto;
            padding: 15px;
            gap: 15px;
        }

        video {
            width: 94%;
            height: 94%;
            object-fit: cover;
            border: 2px solid rgb(95, 89, 89);
            border-radius: 20px;
        }

        .toggle-btn {
            display: inline-block;
            padding: 10px 20px;
            font-size: 16px;
            font-weight: bold;
            text-align: center;
            text-decoration: none;
            color: #fff;
            background-color: #4caf50;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .toggle-btn:hover {
            background-color: #45a049;
        }

        .toggle-btn i {
            margin-right: 5px;
        }
        #btns_div{
            width: 50%;
            margin: auto;
            display: flex;
            justify-content: space-evenly;
            text-align: center;
            padding: 10px;
            background-color: #c4c1c1;
        }
    </style>
</head>
<body>
    <div id="video_grid"></div>
    <div id="btns_div">
        <button id="audio" class="toggle-btn"><i class="fas fa-volume-up"></i> Toggle Audio</button>
        <button id="camera" class="toggle-btn"><i class="fas fa-video"></i> Toggle Camera</button>
    </div>
</body>
</html>
